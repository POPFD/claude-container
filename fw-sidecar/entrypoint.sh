#!/bin/bash
# Placeholder entrypoint for fw-sidecar. Replaced in step 5 with the
# real firewall bootstrap + reconcile loop.
set -euo pipefail
echo "fw-sidecar: placeholder entrypoint (step 3); real logic lands in step 5"
exec tail -f /dev/null
